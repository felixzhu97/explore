{
  "name": "explore-iam",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "explore-iam",
  "projectType": "application",
  "tags": ["type:app", "scope:explore", "lang:java"],
  "implicitDependencies": ["explore-bom"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -f build.gradle.kts && test -f package.json && echo explore-iam nx project ok",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -f gradlew && echo explore-iam test stub ok — run ./gradlew test locally",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -f gradlew && echo explore-iam build stub ok — run ./gradlew build locally",
        "cwd": "{projectRoot}"
      }
    }
  }
}
