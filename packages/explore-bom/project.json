{
  "name": "explore-bom",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/explore-bom",
  "projectType": "library",
  "tags": ["type:bom", "scope:explore", "lang:java"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -f build.gradle.kts && echo explore-bom ok",
        "cwd": "{projectRoot}"
      }
    }
  }
}
